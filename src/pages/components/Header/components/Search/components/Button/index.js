/* Core */
import React from 'react';

/* Presentational */
import IconSearch from 'react-icons/lib/go/search';

const Button = props => (
  <IconSearch
    size={20}
    {...props}
  />
);

export default Button;
